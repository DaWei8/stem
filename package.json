{
  "name": "stem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n \"frontend,logic\" -c \"blue,green\" \"npm run dev --prefix frontend\" \"npm run watch:logic\"",
    "watch:logic": "node scripts/watch-logic.js",
    "build:logic": "node scripts/build-wasm.js",
    "install:all": "npm install && npm install --prefix frontend",
    "build": "npm run build:logic && npm run build --prefix frontend -- --webpack",
    "setup": "npm run install:all && npm run build:logic"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "next": "^15.1.8",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}