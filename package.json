{
  "name": "stem",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npx concurrently -n \"frontend,logic\" -c \"blue,green\" \"npm run dev -w frontend\" \"npm run watch:logic\"",
    "watch:logic": "node scripts/watch-logic.js",
    "build:logic": "node scripts/build-wasm.js",
    "install:all": "npm install && npm install --prefix frontend",
    "setup": "npm run install:all && npm run build:logic"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
