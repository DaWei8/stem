{
  "name": "stem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend & node scripts/watch-logic.js",
    "watch:logic": "node scripts/watch-logic.js",
    "build:logic": "node scripts/build-wasm.js",
    "install:all": "npm install && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "setup": "npm run install:all && npm run build:logic"
  },
"devDependencies": {
    "concurrently": "^8.2.2",
    "next": "^16.2.6",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  }
}